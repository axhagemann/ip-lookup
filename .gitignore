# TLS certificates and private keys — never commit these
certbot/

# Secrets
.env

# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Impeccable local hook cache (session state, not project config)
.impeccable/hook.cache.json
